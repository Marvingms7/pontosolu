import 'package:flutter/material.dart';

class Cronogramas extends StatelessWidget {
  const Cronogramas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
